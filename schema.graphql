type GenzeeToken @entity {
  id: ID!
  tokenID: BigInt!
  owner: User!
  stakedAt: BigInt
}

type User @entity {
  id: ID!
  tokens: [GenzeeToken!]! @derivedFrom(field: "owner")
  genzeeBalance: Int!
  oddxBalance: BigInt!
}
